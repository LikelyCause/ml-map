# Backend dependencies for ML-Map. Install into a Python 3.12 environment.
#
# PyTorch must come from the CUDA index (GPU build). Install it FIRST:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
# then:
#   pip install -r backend/requirements.txt

# --- web ---
fastapi==0.137.2
uvicorn==0.49.0
python-multipart==0.0.32
requests==2.34.2

# --- geospatial / STAC ingest ---
numpy==2.4.6
pillow==12.2.0
rasterio==1.5.0
rioxarray==0.22.0
shapely==2.1.2
geopandas==1.1.3
pystac-client==0.9.0
planetary-computer==1.0.0
odc-stac==0.5.2

# --- models / inference ---
# torch / torchvision: see header (CUDA index). Pinned: torch==2.6.0+cu124, torchvision==0.21.0+cu124
transformers==5.12.1
accelerate==1.14.0
safetensors==0.8.0
timm==1.0.27
huggingface-hub
terratorch==1.2.8
opencv-python-headless==4.13.0.92
